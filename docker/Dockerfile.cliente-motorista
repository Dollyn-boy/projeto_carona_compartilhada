# TODO: Dockerfile do cliente motorista.
#
#   FROM golang:1.26-alpine AS build
#   WORKDIR /app
#   COPY . .
#   RUN go build -o /cliente-motorista ./cmd/cliente-motorista
#
#   FROM alpine:latest
#   COPY --from=build /cliente-motorista /cliente-motorista
#   ENTRYPOINT ["/cliente-motorista"]
#
# Lembrete: no teste final, cliente e servidor rodam em maquinas
# distintas do laboratorio — o endereco do servidor nao pode ser
# "localhost", deve ser configuravel (variavel de ambiente ou flag).
